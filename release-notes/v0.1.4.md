# v0.1.4

## Highlights
- Extensive internal refactoring to improve code modularity and error handling.
- Migration to `clap` for command-line argument parsing.

## Changed
- Migrated CLI to use `clap` for command parsing.
- Standardized error types throughout the application.
- Split TUI app state into smaller, focused modules.
- Extracted shared UI widget helpers.
- Separated domain schema parsing from validation logic.
- Added environment repository abstraction.
- Cleaned up unused exports and adapters.
