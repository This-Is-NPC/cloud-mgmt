# v0.1.6

## Highlights
- Patch releases now tag automatically on merges to `main` with required release notes.
- Added a contributor guide with setup and PR workflow tips.

## Added
- Auto-release workflow that bumps `Cargo.toml` and tags the next patch version.
- `CONTRIBUTING.md` with local run/test instructions and release notes guidance.

## Changed
- Release process requires `release-notes/vX.X.X.md` before tagging.
